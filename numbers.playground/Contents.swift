import UIKit

// Numbers
// Type inference (recommended)
var age = 42

// Explicitly declared type
var weight: Int = 155 // Int

// Arithmatic Operators
// + - / *
var area = 15 * 20
var sum = 5 + 6
var diff = 10 - 3
var div = 12 / 3
var div2 = 13 / 5
var remainder = 13 % 5
var result = "The result of 13 / 5 is \(div2) with a remainder of \(remainder)"

var randomNumber = 13
if randomNumber % 2 == 0 {
    print("this is an even number")
} else {
    print("this is an odd number")
}
